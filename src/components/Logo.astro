---
import { SITE } from 'astrowind:config';
import { Image } from 'astro:assets';
import logo from '~/assets/images/cremins-lab-logo.png';
---

  <Image 
    src={logo} 
    alt={`${SITE?.name} logo`} 
    width={600} 
    height={600} 
  />
